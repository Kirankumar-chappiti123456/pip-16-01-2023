#include<stdio.h>
int main()
{
   int n=121,temp,r=0,sum=0;
   temp=n;
   while(n>0)
   {
       r=n%10;
       sum=(sum*10)+r;
       n=n/10;
   }
   (temp==sum)?printf("palindrome"):printf("not palindrome");
}
