#include<stdio.h>
int main()
{
    int a[]={1,2,4,5,6,7,8,9,10};
    int l=sizeof(a)/sizeof(a[0]);
    int i=0,j=0,sum=0;
    for(i=0;i<l;i++)
    {
       sum=sum+a[i];
    }
    int k=(l+1)*(l+2)/2-sum;
    printf("the missing elemenet is:%d",k);
}
